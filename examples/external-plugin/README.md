# smoothie-extra

An **external** plugin shipped as its own uv project, proving the from-scratch
framework discovers third-party packages exactly like pluggy does - through the
`kitchen` entry-point group and `importlib.metadata`, with no host changes.
